name: Firebase Continuous Deployment

on:
  push:
    branches: [ master, feature, feature1 ]


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
        with:
          node-version: 14
      - run: npm ci
      - run: npm run build
      